'use strict';

/**
 * initial-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::initial-subscription.initial-subscription');
